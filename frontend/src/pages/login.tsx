import { LoginForm } from "@/components/loginFomr";
import React, { FC } from "react";

export const Login: FC = () => {
    return (
        <div className="mx-auto">
            <LoginForm />
        </div>
    )
};